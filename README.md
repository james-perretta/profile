# Terminal Profile
To install, clone the repo, cd into `profile`, and run `./install.sh`, then `source ~/.bashrc`. 
This will install our bash_aliases and refresh the profile in the current session.

Add anything specific to individual machines to `local.bash`.
This file is not committed to the repo.